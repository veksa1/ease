# ALINE Service Environment Variables
# Copy this file to .env for local development

# ============================================================================
# Server Configuration
# ============================================================================

# Port for the FastAPI service (Cloud Run uses 8080 by default)
PORT=8000

# ============================================================================
# External Services
# ============================================================================

# n8n webhook URL for calendar integration (optional)
# If not set, calendar features will be disabled
# Example: https://your-n8n-instance.com/webhook/aline-context
N8N_WEBHOOK_URL=http://localhost:5678/webhook/aline-context

# OpenWeather API Key (Ticket 023)
# Sign up at https://openweathermap.org/api
# Free tier: 1,000 calls/day, sufficient for MVP
OPENWEATHER_API_KEY=your_openweather_api_key_here

# OpenWeather cache TTL in seconds (default: 3600 = 1 hour)
OPENWEATHER_CACHE_TTL=3600

# ============================================================================
# Google Cloud Configuration (for production deployment)
# ============================================================================

# These are configured as GitHub Secrets, not in .env file
# See DEPLOYMENT_QUICKSTART.md for setup instructions
#
# GCP_PROJECT_ID=your-project-id
# GCP_SA_KEY=<service-account-json-key>

# ============================================================================
# Database Configuration
# ============================================================================

# SQLite database path (default: data/aline.db)
# In production, this could point to Cloud SQL or other database
# DATABASE_PATH=data/aline.db

# ============================================================================
# Model Configuration
# ============================================================================

# Model checkpoint path (default from configs/service.yaml)
# MODEL_CHECKPOINT_PATH=runs/checkpoints/best.pt

# Device for model inference: 'cuda', 'cpu', or 'auto'
# Default is 'auto' which uses GPU if available
# DEVICE=auto

# ============================================================================
# Development Settings
# ============================================================================

# Enable hot reload for development (not recommended for production)
# RELOAD=false

# Number of uvicorn workers (1 for development, increase for production)
# WORKERS=1

# ============================================================================
# Security (Production)
# ============================================================================

# CORS allowed origins (comma-separated)
# For production, replace * with your frontend domain
# CORS_ORIGINS=*

# API keys or authentication tokens (if implementing authentication)
# API_KEY=your-secret-api-key

# ============================================================================
# Logging
# ============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# ============================================================================
# Feature Flags
# ============================================================================

# Enable/disable calendar integration
# ENABLE_CALENDAR=true

# Enable/disable experimental features
# ENABLE_EXPERIMENTAL=false
